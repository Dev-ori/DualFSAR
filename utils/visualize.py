import torch.distributed as dist
import warnings

def visualize(img, pred, targets, logger=None):
    warnings.warn("Implement visualization method, if you need to visualize the results", stacklevel=1)
    return None